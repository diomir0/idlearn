name: idlearn
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - numpy
  - requests
  - pip
  - pip:
      - pymupdf
      - customtkinter
      - genanki
      - streamlit
      - ollama
      - python-dateutil
